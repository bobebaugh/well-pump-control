# Tab5 test fixtures

rules-runtime-package-v3-checkpoint1.json is the current test package used by the
host suite; its filename is retained so existing tests stay stable. The legacy
control fixture exercises rejection of direct-RLY0 packages. Neither is a deployed
package or an installation source. Shelly component-response fixtures label their
captured/documentary evidence. Preserve that distinction when adding cases.
